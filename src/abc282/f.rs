#![allow(dead_code, unused_imports)]
use proconio::input;
use itertools::Itertools;
use std::cmp::{max, min};
use std::cmp::Ordering;
use std::collections::VecDeque;
use std::collections::BinaryHeap;
use std::collections::HashMap;
use std::collections::HashSet;
use proconio::marker::Chars;
use std::f64::consts::PI;
use std::mem::swap;
use superslice::*;
fn main() {
    input! {
        
    }
            // if !hash_map.contains_key(&(u_i-1)) {
        //     let mut hash_map2 = HashMap::new();
        //     hash_map2.insert(v_i - 1, 1);
        //     hash_map.insert(u_i-1, hash_map2);
        // }
        // else {
        //     (*hash_map.get_mut(&(u_i-1)).unwrap()).insert(v_i-1, 1);
        // }
        // if !hash_map.contains_key(&(v_i-1)) {
        //     let mut hash_map2 = HashMap::new();
        //     hash_map2.insert(u_i - 1, 1);
        //     hash_map.insert(v_i-1, hash_map2);
        // }
        // else {
        //     (*hash_map.get_mut(&(v_i-1)).unwrap()).insert(u_i-1, 1);
        // }

}